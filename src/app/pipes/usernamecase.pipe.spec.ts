import { UsernamecasePipe } from './usernamecase.pipe';

describe('UsernamecasePipe', () => {
  it('create an instance', () => {
    const pipe = new UsernamecasePipe();
    expect(pipe).toBeTruthy();
  });
});
